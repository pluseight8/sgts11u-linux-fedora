# id|label|target|freedesktop icon name
# Targets are stable Android intents by default. A user may add an exact
# package target as `package:org.example.app`; the project never discovers,
# installs, disables or modifies packages and does not assume Samsung/Google
# package names that can vary by firmware or region.
android-settings|Android Settings|intent:android.settings.SETTINGS|preferences-system
wifi-settings|Wi-Fi settings|intent:android.settings.WIFI_SETTINGS|network-wireless
bluetooth-settings|Bluetooth settings|intent:android.settings.BLUETOOTH_SETTINGS|bluetooth
camera|Camera capture|intent:android.media.action.IMAGE_CAPTURE|camera-photo
# Example user-selected package entry (disabled until the user supplies it):
# firefox|Firefox|package:org.mozilla.firefox|web-browser
